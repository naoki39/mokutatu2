<%= link_to image_tag(post.user.image.variant(resize: '500x500'), class: "posts_user_icon"), user_path(post.user.id) %>
 <%= link_to post_path(post.id) do %>
     <%= post.user.nickname %>
      <%= post.text %>
     <%= l post.created_at %>
<% end %>
